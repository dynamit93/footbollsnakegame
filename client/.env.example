# Required for production builds (e.g. Vercel). Origin only, no trailing slash.
# Example: https://football-snake-api.onrender.com
VITE_SERVER_URL=

# Optional: Socket.IO API URL when running `vite dev` (default http://localhost:3001)
# VITE_DEV_SERVER_URL=http://localhost:3001
