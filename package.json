{
  "name": "soccer-snake",
  "private": true,
  "version": "1.0.0",
  "description": "Soccer Snake — grid multiplayer game",
  "scripts": {
    "dev": "npm run build -w shared && concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npx wait-on tcp:127.0.0.1:3001 && npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "start": "npm run start -w server"
  },
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "~5.9.3",
    "wait-on": "^8.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
